import { PageMetaDtoParameters } from './page-meta-dto-parameters';

describe('PageMetaDtoParameters', () => {
  it('should be defined', () => {
    expect(new PageMetaDtoParameters()).toBeDefined();
  });
});
